version: v1.0
name: Verification Pipeline
agent:
  machine:
    type: s1-linux-aws
    os_image: ""

blocks:
  - name: Build
    task:
      jobs:
        - name: Build Project
          commands:
            - echo Build

  - name: Verification
    run:
      when: 'change_in('/src')'
    task:
      jobs:
        - name: Analyze Code
          commands:
            - echo Verify
